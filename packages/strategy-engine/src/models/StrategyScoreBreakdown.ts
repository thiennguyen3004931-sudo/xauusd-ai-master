export interface StrategyScoreBreakdown {
  signal: number;
  structure: number;
  regime: number;
  momentum: number;
  location: number;
  multiTimeframe: number;
  session: number;
  total: number;
}
